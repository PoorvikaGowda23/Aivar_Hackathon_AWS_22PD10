version: 1.0
runtime: python310
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  command: uvicorn app.main:app --host 0.0.0.0 --port 8000
  network:
    port: "8000"
  env:
    - name: LOG_LEVEL
      value: INFO
